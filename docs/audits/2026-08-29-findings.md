# Findings punch list — 2026-08-29

Companion to [2026-08-29-full-codebase-audit.md](./2026-08-29-full-codebase-audit.md). Same IDs. Severity is impact if this v1 is used as the docs say it should be used.

## Critical

| ID | Finding |
|----|---------|
| NC-001 | Invariant 8: invalid trees still reach `<Renderer>`. Validation is in `useLayoutEffect` (after paint commit), not before render. |
| NC-002 | Invariant 12: React renders raw `tree`; observer renders Zod-stripped `result.data`. `extraRegistry` desyncs them further. |
| NC-003 | Spec Rule 3: same field ID, different component type is not a catalog error. Staging type-confuses. |
| NC-004 | Spec Open Question 2 forbade shipping backpressure as silent drop. Shipped as silent drop. Flag is not even public. |
| NC-005 | `emitIntent` JSDoc says the promise never rejects and also that handler rejections propagate. Implementation rejects. |

## High

| ID | Finding |
|----|---------|
| NC-006 | Unsafe `element.props as` casts on trees that failed validation (follows from NC-001). |
| NC-007 | Catalog `cancel` claims to discard staging. Nothing does. |
| NC-008 | `Button.action.name` is `z.string()`, not catalog actions. Open action namespace. |
| NC-009 | `createNCRuntime.catalogVersion` and `NCRenderer.catalogVersion` can diverge. |
| NC-010 | Invariant 9 depends on `useCommittedTree`; `NCApp` uses `useState`. Primary path has no atomic-commit protection. |
| NC-011 | React in `dependencies`, not `peerDependencies`. Dual-React hook failure waiting in production. No `exports` map. |
| NC-012 | `"use client"` on leaves, not on the tsup entry. Next.js App Router will treat the barrel as a Server Component. |
| NC-013 | `getLastRender()` returns a mutable shared reference. |
| NC-014 | `defaultNCProjection` assigns `obj[entity.name]` on a prototype-bearing object. Pollution gadget. Duplicate names silent-overwrite. |
| NC-015 | `serialize("html")` plus staging `currentValue` is an XSS footgun if used as `innerHTML`. JSDoc does not warn. |
| NC-016 | No max length on staging strings or intent/observer payloads. One paste fills an LLM prompt. |
| NC-017 | `destroy()` / NCApp unmount do not cancel in-flight handlers or uninstall `setTree`. |
| NC-018 | CI checks out JSON-UI and memoryjs at floating default branch. Real API is unpinned. |

## Medium

| ID | Finding |
|----|---------|
| NC-019 | `extraRegistry` can replace `Button` and reintroduce dropped `action.params`. `NCApp` does not plumb `extraRegistry`. |
| NC-020 | Runtime catalog vs renderer catalog not identity-checked. |
| NC-021 | Catch block says "buffer untouched" after `reconcile()` may already have run. |
| NC-022 | Strict Mode double-runs observer; passId is an effect counter, not a commit counter. Tests do not use StrictMode. |
| NC-023 | Buffer-isolation regex misses `require`, dynamic `import()`, subpaths. Promised ESLint rule does not exist. |
| NC-024 | `NCCatalogVersion` brand is forgeable with `as`. |
| NC-025 | Field `id: z.string()` allows empty and `__proto__`. |
| NC-026 | `NCApp` ignores `initialTree` after mount. Undocumented. |
| NC-027 | Headless Button comment claims params are pre-resolved; component just copies props. Unverified vs JSON-UI. |
| NC-028 | Observer `currentValue` after a React re-commit is not tested through `NCRenderer`. |
| NC-029 | `createNCRuntime` is async with no `await`. |
| NC-030 | No Error Boundary, no `onValidationError`. Failures are `console.warn`. |
| NC-031 | Clicks are fire-and-forget. No pending UI. |
| NC-032 | Tests leak in-flight promises; `Date.now()` + `setTimeout(5)` flake risk. |
| NC-033 | `nc-types.test.ts` does not assert `NCRuntime.observer`. |
| NC-034 | `input-components.test.tsx` does not test NCButton execute/params. COMPONENTS.md claims it does. |
| NC-035 | No test that a throwing handler clears `intentInFlight`. |
| NC-036 | emit/setHandler-after-destroy untested. |
| NC-037 | Projection hides relations except a count; missing timestamps become `""`. |
| NC-038 | `docs:deps` needs nested `js-yaml`; generated architecture docs omit `src/observer/` and are stale. |
| NC-039 | Bun lockfile + README `npm install` + Node `>=18` vs CI Node 22. No example app. `dev` watches a barrel. |
| NC-040 | ESLint config references uninstalled packages, empty rules, no lint script. CI `--if-present lint` no-ops. |
| NC-041 | `passWithNoTests: true` leftover from scaffold. |
| NC-042 | `skipLibCheck: true` hides sibling-type drift. |
| NC-043 | No Prettier config; format not checked in CI. |
| NC-044 | Dependabot auto-merges minor/patch, including unused `@anthropic-ai/sdk`. |
| NC-045 | README quickstart uses inline `buildIntentHandler`; tests do too; AGENTS.md says they do not. |
| NC-046 | Both shipped specs still say "not yet implemented." Leaked `C:\Users\danie\Dropbox\...` path. |
| NC-047 | CHANGELOG all `[Unreleased]`; Known deferred still says headless is unmounted. |
| NC-048 | CLAUDE.md / AGENTS.md / COMPONENTS.md counts (tests, invariants, exports, files) contradict the tree. |
| NC-049 | State surfaces counted as 5, 6, or 7 depending on the file. AGENTS.md still says "do not invent" past five. |
| NC-050 | Invariant 7 original file path is dead; isolation test guards a stub. |
| NC-051 | Risk 1 (LLM acceptance contract) has no prompt and no adversarial "wrong tree" tests. |
| NC-052 | No threat model. LLM trees are untrusted UI. DynamicValue can copy durable paths into action params. |

## Low

| ID | Finding |
|----|---------|
| NC-053 | Starter components: no `aria-invalid`, no submit-on-enter, no focus restore, no pending state. |
| NC-054 | `data-key` vs React `key` vs field `id` are three namespaces, undocumented. |
| NC-055 | No layout system; Container is an unstyled div. |
| NC-056 | TextField is single-line text only. No textarea/select despite spec mentions. |
| NC-057 | `.dropboxignore` author-machine leftover. |
| NC-058 | Nested tool package-lock vs root bun.lock; `@types/node` 20 vs 25. |
| NC-059 | Dual ESM/CJS build not wired via `exports`; no `files` field. |
| NC-060 | No `sideEffects: false`. |
| NC-061 | `z.unknown()` action params; in-process trees skip JSON constraints. |
| NC-062 | Stub handler does not validate `nextTree` against the catalog. |
| NC-063 | `serialize` throws on bad format; `render` never throws. Mixed. |
| NC-064 | `getConsecutiveFailures` has no subscriber; stub never reads it. |
| NC-065 | Dual registry pass papers over vestigial JSONUIProvider prop. |
| NC-066 | `as ComponentRenderer` casts; `skipLibCheck` will hide upstream shape changes. |
| NC-067 | All-or-nothing dependencies (headless + react + memoryjs + unused Anthropic SDK). |
| NC-068 | `integration.test.tsx` not colocated. |
| NC-069 | README shows `from "neural-computer"`; package is `private: true` and unpublished. |
| NC-070 | DATAFLOW.md missing `observer.render` and incomplete effect deps. |
| NC-071 | INVARIANTS.md Invariant 7 prose omits `../observer`. |
| NC-072 | Runtime unit tests omit `catalog_version` on synthetic events. |
| NC-073 | `useCommittedTree` tests are JSON-UI stream-protocol tests. |
| NC-074 | `test:coverage` has no coverage provider dependency. |
| NC-075 | No SPDX headers (acceptable under Apache-2.0). |
| NC-076 | Module-level HTML serializer singleton. |
| NC-077 | `currentValue` compactness vs `JSON.stringify` omitting `undefined`. |
| NC-078 | First paint of `initialTree` is unvalidated. |
| NC-079 | NCApp effect has no cleanup (see NC-017). |
| NC-080 | Public `registry` override on observer is a production footgun. |
| NC-081 | Headless `meta.visible` hardcoded true. |
| NC-082 | Source comments duplicate CHANGELOG and will drift. |
| NC-083 | April-11 plan still looks current if an agent globs `docs/plans/`. |
| NC-084 | No CODEOWNERS. |
| NC-085 | CI does not format-check. |
| NC-086 | npm `overrides` do not protect consumer React trees. |
| NC-087 | Observer on the layout path is not "no perf cost." |
| NC-088 | No `React.memo`; possible remount/focus loss on commit (upstream-dependent). |
| NC-089 | Projection is full rebuild every snapshot. |
| NC-090 | Observation payload duplicates user data (tree + snapshot + projection). |
| NC-091 | Vitest `globals: true` while tests still import `vitest`. |
| NC-092 | Package description and README describe an LLM+Python runtime. What ships is a form widget, a stub, and a cache. |

## Counts

- Critical: 5
- High: 13
- Medium: 34
- Low: 40
- Total: 92
